{
  "name": "vibecheck",
  "version": "1.0.0",
  "description": "VibeCheck - AI-powered group formation tool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "python3 -m pip install --user -r requirements.txt || echo 'Python pip install failed, continuing...'"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
