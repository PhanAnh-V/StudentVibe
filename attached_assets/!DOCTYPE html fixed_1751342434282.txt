<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Mystery Generator</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <div class="form-container">
        <div class="form-header">
            <h1 class="form-title">Mystery Generator</h1>
            <p class="form-description">Answer 6 questions to discover your creative archetype and connect with your vibe squad!</p>
        </div>

        <div class="form-card">
            <form method="POST" action="{{ url_for('submit_questionnaire') }}">
                {{ form.hidden_tag() }}

                <!-- Flash Messages -->
                {% with messages = get_flashed_messages(with_categories=true) %}
                    {% if messages %}
                        {% for category, message in messages %}
                            <div class="alert alert-{{ category }}">
                                {{ message }}
                            </div>
                        {% endfor %}
                    {% endif %}
                {% endwith %}

                <!-- Name Field -->
                <div class="form-group">
                    <label for="name" class="form-label">Name <span class="required-asterisk">*</span></label>
                    {{ form.name(class="form-control", id="name") }}
                    {% if form.name.errors %}
                        <div class="form-error">
                            {% for error in form.name.errors %}
                                <small>{{ error }}</small>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>

                <!-- Questions Section -->
                <div class="form-group">
                    <h3>Mystery Generator Questions</h3>
                    {% if questions %}
                        {% for i in range(6) %}
                            {% set question_field = 'question' + (i + 1)|string %}
                            {% set question_data = questions[i] if i < questions|length else {'title': 'Question ' + (i + 1)|string, 'description': 'Please answer this question.'} %}
                            <div class="question-group">
                                <h4 class="question-title">{{ question_data.title }}</h4>
                                <p class="question-description">{{ question_data.description }}</p>
                                {{ form[question_field](class="form-control") }}
                                {% if form[question_field].errors %}
                                    <div class="form-error">
                                        {% for error in form[question_field].errors %}
                                            <small>{{ error }}</small>
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>
                        {% endfor %}
                    {% else %}
                        <div class="question-group">
                            <h4 class="question-title">Question 1</h4>
                            <p class="question-description">Please answer this question.</p>
                            {{ form.question1(class="form-control") }}
                            {% if form.question1.errors %}
                                <div class="form-error">
                                    {% for error in form.question1.errors %}
                                        <small>{{ error }}</small>
                                    {% endfor %}
                                </div>
                            {% endif %}
                        </div>
                        <div class="question-group">
                            <h4 class="question-title">Question 2</h4>
                            <p class="question-description">Please answer this question.</p>
                            {{ form.question2(class="form-control") }}
                            {% if form.question2.errors %}
                                <div class="form-error">
                                    {% for error in form.question2.errors %}
                                        <small>{{ error }}</small>
                                    {% endfor %}
                                </div>
                            {% endif %}
                        </div>
                    {% endif %}
                </div>

                <!-- Additional Information -->
                <div class="form-group">
                    <h3>Additional Information</h3>
                    <div class="form-group">
                        <label for="country" class="form-label">Country</label>
                        {{ form.country(class="form-control", id="country") }}
                        {% if form.country.errors %}
                            <div class="form-error">
                                {% for error in form.country.errors %}
                                    <small>{{ error }}</small>
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>
                    <div class="form-group">
                        <label for="gender" class="form-label">Gender</label>
                        {{ form.gender(class="form-control", id="gender") }}
                        {% if form.gender.errors %}
                            <div class="form-error">
                                {% for error in form.gender.errors %}
                                    <small>{{ error }}</small>
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>
                </div>

                <!-- Submit Button -->
                <div class="submit-group">
                    {{ form.submit(class="btn btn-primary btn-lg") }}
                </div>
            </form>
        </div>
    </div>
</body>
</html>